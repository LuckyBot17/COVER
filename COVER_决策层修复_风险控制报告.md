# COVER 决策层修复：风险控制（Neyman–Pearson）实验报告

> 配套图：`COVER_Rbench决策层修复_风险控制.png` ｜ 数据：`risk_control_rbench.json`
> 日期：2026-08-21 ｜ 模型：LLaVA-1.5-7B ｜ 基准：R-Bench 全量干净声明（1331 条打分，eval 812 = 692 真 / 120 假）
> 服务器：`/root/autodl-tmp/cover_min_exp/risk_control_rbench.py` → `risk_control_rbench.json`、`run_risk_control.log`

## 摘要（结论先行）

**修复部分成功。** 把决策层的接受阈值从"真声明覆盖分位"改成"假声明风险分位"之后，出现了两个明确结果：

1. **在闸门最强的格子（非空间族高置信度档）拿到了可工作的操作点**——这正是旧决策层风险 1.000（全部假声明被接受）的地方。风险控制下（α=0.10，档内标定），COVER 在风险 0.33 时保住 67% 的真声明覆盖，而 raw 置信度在相近风险下只保住 7%。**同一个风险预算里，COVER 找到的真声明是 raw 的十倍。**
2. **全局上 COVER 在每个 α 都小幅优于 raw**（非空间 α=0.10：覆盖 0.52 vs 0.20），但**风险保证是松的**——COVER 实测风险约超名义 α 一倍（复合分数的跨图校准问题），raw 则贴得住。

两个没有修好的东西必须如实记录：**中置信度档对两个分数都崩（覆盖 ~0.04）**，风险控制系统在那里选择几乎全部 ABSTAIN；**REPAIR 轴不受阈值修复影响**（风险控制下 REPAIR 触发更多、仍 92% 落在真声明上），它需要的是 GQA `present_rels` 的真值测试台，不是阈值调参。

---

## 1. 修的是什么：从"保覆盖"到"控风险"

旧决策层（`decision_layer_rbench.py`）的失败机制很具体：接受阈值 q 取在**真**声明得分的 (1−δ) 分位上——这是 split-conformal 的"覆盖保证"操作点，回答的是"别把真话漏掉"。但假声明的得分平均排在真声明下面，于是所有排位在真分布中部以下的声明（包括几乎全部假声明）都被 q 收进来，高档风险因此达到 1.000。规则回答错了问题：我们要的是"拒绝假话"，它保证的是"别漏真话"。

修复（Neyman–Pearson / 风险控制）：q 取在**假**声明得分的 α 分位上——接受 iff score ≤ q，q = 校准半集假声明得分的第 α 个经验分位（有限样本 conformal，k = max(1, ⌊α(n+1)⌋)），由可交换性保证 E[P(ACCEPT|假)] ≤ α。实验问的问题是修复是否可行的唯一判据：**在受控风险 α 下，COVER 能保住多少真声明覆盖，相比 raw 置信度如何。**

**协议**（与旧决策层同构，纯分析、零新推理）：calib 图像按 seed 2026 对半拆为 标准拟合（b/g）与 conformal 标定（q 分位）；eval 图像与两者不相交。分数沿用 `scored_rbench_full.csv` 的 6 算子差分与 `a_orig`；REPAIR 翻转用 `a_inv_orig.csv`。主协议 q 用 conf 半集假声明标定；另加一个敏感性——q 用**全部** calib 假声明标定（样本更大，eval 与 calib 图像不相交，保证仍成立）。COVER 分数取 λ=0（μ 口径，与闸门一致）。

## 2. 主结果：全局风险-覆盖曲线

表 1：主协议（q 由 conf 半集假声明标定）。行格式为 覆盖 P(ACCEPT|真) ／ 实测风险 P(ACCEPT|假)。

| scope | 方法 | α=0.05 | α=0.10 | α=0.20 | α=0.50 |
|---|---|---|---|---|---|
| 非空间 (34 假标定) | COVER λ=0 | 0.095 / 0.015 | 0.466 / 0.194 | 0.723 / 0.373 | 0.909 / 0.642 |
| | raw 置信度 | 0.066 / 0.045 | 0.197 / 0.075 | 0.565 / 0.313 | 0.836 / 0.612 |
| 全部 (42 假标定) | COVER λ=0 | 0.278 / 0.108 | 0.458 / 0.225 | 0.642 / 0.350 | 0.870 / 0.583 |
| | raw 置信度 | 0.072 / 0.050 | 0.332 / 0.158 | 0.536 / 0.300 | 0.821 / 0.600 |
| 空间 (8 假标定) | COVER λ=0 | 0.301 / 0.226 | 0.301 / 0.226 | 0.301 / 0.226 | 0.746 / 0.566 |
| | raw 置信度 | 0.263 / 0.189 | 0.263 / 0.189 | 0.263 / 0.189 | 0.727 / 0.528 |

**读法**：在同一 α 上，COVER 的覆盖在非空间与全部 scope 里都高于 raw（α=0.10 非空间 0.466 vs 0.197），代价是 COVER 实测风险也更高（0.194 vs 0.075）。按**匹配的实测风险**比更公平：非空间在风险 ~0.31–0.37 处，COVER 覆盖 0.72 vs raw 0.57；在风险 ~0.60 处，COVER 0.91 vs raw 0.84。结论是**全局小幅占优，不是碾压**。空间族两者都基本一样（样本太少，8 条假声明标定，前四个 α 共用同一个 q）。

## 3. 关键结果：分档风险控制——修复在目标格子里成立

表 2：非空间族按置信度分档（档边界由全部 calib 的 a_orig 三分位定），档内风险控制 α=0.10。使用敏感性标定（q 由全部 calib 假声明定标，样本更大、更稳）。

| 档 | 标定假声明 | eval 真/假 | COVER 覆盖（风险） | raw 覆盖（风险） |
|---|---|---|---|---|
| 低 | 36 | 142 / 37 | 0.345（0.162） | 0.120（0.081） |
| 中 | 17 | 159 / 21 | 0.038（0.000） | 0.019（0.095） |
| **高** | 10 | 182 / 9 | **0.670** [0.62, 0.72]（0.333） | **0.066** [0.04, 0.09]（0.222） |

这是修复报告的主角。非空间高置信度档——闸门里 raw 反转 0.466、μ 分离 0.713 的格子，也是旧决策层风险 1.000 的地方——在风险控制下：**COVER 用 0.33 的假接受风险保住 0.67 的真声明覆盖；raw 在相近风险下只保住 0.07。** 同一个风险预算内，COVER 找回的真声明是 raw 的十倍，而且覆盖的 95% CI 完全不重叠（[0.62,0.72] vs [0.04,0.09]）。主协议（conf 半集标定，仅 3 条假声明）也一致：COVER 0.77 vs raw 0.18。**闸门的分隔力第一次变成了可工作的决策操作点，且恰好在 raw 失效的场景里兑现。**

中档是诚实的反例：两个分数在风险控制下都近乎全 ABSTAIN（覆盖 0.02–0.04）。中档是闸门里最没有分隔力的格子（μ AUROC 0.635、raw 0.585），风险控制的正确行为就是"分不清就别说"——这是保守的合理表现，不是 bug，但它说明 COVER 的中档仍需更强的分数。

## 4. 敏感性：全部 calib 假声明标定，方向不变

q 改用全部 calib 假声明（非空间 63 条）后，全局曲线：COVER α=0.10 覆盖 0.522（风险 0.194）、α=0.20 覆盖 0.662（风险 0.313）；raw α=0.10 覆盖 0.197（风险 0.075）、α=0.20 覆盖 0.499（风险 0.269）。**COVER 在每个 α 的覆盖都高于 raw，且高档结论（表 2）完全站住。** 敏感性证明主结果不是 conf 半集那 34 条假声明的偶然。

## 5. 没修好的：三个必须说的失败面

**COVER 的风险保证是松的（配图 d）**。主协议下 COVER 实测风险普遍超名义 α 约一倍（非空间 α=0.10 名义 0.10 → 实测 0.194；α=0.20 → 0.373），raw 则贴住名义值。原因是 COVER 的 A 是六个算子的复合分（含 b/g 标准化），跨图像集的分布漂移比单维的 a_orig 大，标准化的中位数口径盖不住这种漂移。含义：当前 A 不能直接给出严格的风险上界，需要用更多标定数据、或加一步跨图像重标定、或对 A 做保序变换后再套风险控制。**注意这不是分数失效**——覆盖排序仍然成立（COVER 在同样实测风险下覆盖更高），只是风险数字不能按名义值对外宣称。

**中档双崩**（表 2 中档）。两个分数在风险控制下都只能保住 2–4% 的覆盖。风险控制系统在无法分离的档里正确选择了沉默，但要真正可用，中档需要更强的判别分数。

**REPAIR 轴没被阈值修复（配图之外的表格）**。双候选集 {r_gen, r⁻¹} 在风险控制下：非空间 α=0.10 时 REPAIR 触发 74 次（旧覆盖保证下只有 32 次），其中 **68 次落在真声明上（92%）**，翻转率 ~100%。阈值收紧反而让更多真声明成为 REPAIR 候选（原声明超 q、逆候选通过）。**REPAIR 的正确性是一个独立问题，阈值怎么调都绕不过去**——必须用 GQA `present_rels`（场景图真实关系）做真值实验，才能知道 REPAIR 修出去的那个关系到底对不对。

## 6. 结论：哪些算修好了，哪些还没

**修好的**：ACCEPT/ABSTAIN 轴在目标场景（非空间高档）有了可工作的操作点——旧决策层 100% 接受假声明的格子，现在以 0.33 风险保住 0.67 覆盖，且远超 raw 的 0.07。全局上 COVER 在每个风险水平都高于 raw。决策层的"操作点失败"在最高价值的位置被定向修复。

**没修好的**：COVER 的风险数字不能当严格上界用（校准偏松，需要重标定）；中档没有判别力；REPAIR 轴的可用性需要 GQA 真值实验。**"修复决策层操作点"是 COVER 成功判定的必要条件中最核心的一项，现在有了第一条正向证据；但不是充分条件——REPAIR 真值、空间族口径、J 的消融这三件事仍然独立地悬着。**

## 7. 下一步（按优先级）

1. **A 的跨图像重标定**：修 COVER 的风险偏松——增大标定样本（已证有效）、或对 A 做保序/分位变换后再控风险，把实测风险拉回名义线内。
2. **GQA `present_rels` REPAIR 真值实验**：唯一能判定 REPAIR 修对没有的测试台，需要新一轮服务器推理（对真实替代关系打分）。
3. **中档分数改进**：中置信度档两者都无分隔力，需要更细的算子或对中档单独建模。
4. **空间族口径**（遗留）：空间族高档 μ 反转，COVER 的论文声明须限定非空间族或单独设计。
5. **重复图像拆分**：用多次随机拆分确认高档 0.67 vs 0.07 的稳定性（当前是单次拆分 + image-clustered CI）。

**复现**：脚本与结果已在服务器 `/root/autodl-tmp/cover_min_exp/`（`risk_control_rbench.py`、`risk_control_rbench.json`、`run_risk_control.log`），本地 `tmp/cover_min_exp/` 有副本与配图生成脚本。
