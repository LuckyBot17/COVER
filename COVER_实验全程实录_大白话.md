# COVER 实验全程实录（大白话版）

> 把这段对话里跑过的所有实验，按顺序讲清楚：**在哪跑的、跑的是什么、得到了什么、对 COVER 意味着成功还是失败**。
> 所有路径都是服务器（v48GB，autodl）上的真实位置，可以照着复现。
> 撰写日期：2026-08-21 ｜ 模型：LLaVA-1.5-7B（fp16，RTX 4090）

---

## 先说背景：COVER 到底在测什么（用大白话）

模型看一张图，说"图里的男人穿着衬衫"。这句话到底真不真？COVER 的想法是这样验证：把衬衫那块、男人那块分别高斯模糊掉，再问模型同样的问题，看它的信心掉多少。

直觉是——真话依赖真部件：遮掉衬衫，"穿着衬衫"的回答应该明显动摇。而幻觉靠的是错误部件，或者干脆是语言惯性（比如男人旁边有条狗，模型就顺口说"狗戴着帽子"，可图里根本没有帽子，遮哪儿它都不慌）。COVER 还加了一个"反着问"的对照（"衬衫被人穿着"），防止模型只是顺着句式答 Yes。

这些"遮了之后掉多少分"的组合，就是方法文档里说的**负控制差分**。把它们标准化以后平均，得到一个"这条声明到底有没有实打实的视觉证据支持"的分数 μ。分数越高、越能区分真假，COVER 的假设就越成立。

这套逻辑要真正变成论文里的方法，得走四步实验阶梯：**先冒烟（能跑吗）→ 再在小规模上验证核心假设（GQA 闸门）→ 再到公开基准上补样本（R-Bench 闸门）→ 最后看决策层能不能真的用起来（ACCEPT / REPAIR / ABSTAIN）**。下面每一节就是一个实验。

**服务器布局**（所有实验都在这里）：

| 东西 | 路径 |
|---|---|
| 实验代码与结果 | `/root/autodl-tmp/cover_min_exp/` |
| 方法文档（§6 决策层、§14 闸门判据） | `/root/autodl-tmp/COVER_完整Method与贯穿案例.md` |
| 模型权重 | `/root/autodl-tmp/llava-1.5-7b/master` |
| GQA 数据（图片+场景图） | `/root/autodl-tmp/data/GQA/` |
| R-Bench 数据（图片+标注） | `/root/autodl-tmp/data/R-bench/` |

---

## 实验一：冒烟测试 —— "东西通不通"

**路径**：`cover_min_exp/smoke/`（GQA 版）、`cover_min_exp/smoke_rbench/`（R-Bench 版），另有 `smoke_rbench_claims.csv`、`smoke_rbench_run.log` 在 `cover_min_exp/` 下。

**实际意义**：装软件、载模型、调接口。验证"模型能加载、能打分、整条流水线不炸"，没有半点科学结论。

**结论**：通了。模型加载正常，Yes/No 打分接口正常，数据准备脚本能跑。也顺手确认了 R-Bench 的数据结构（instance-level，3767 道题自带 s/o 方框和人工标注的真假）。

**对 COVER**：纯工程，不算成败。

---

## 实验二：GQA 最小可证伪实验 —— "核心假设在不在"

**路径**：造数据 `gqa_prepare.py` → 打分 `cover_min_experiment.py` → 遮挡方式消融 `mask_ablation.py`；产出 `claims.csv`、`scored_claims.csv`（灰块版）、`scored_claims_blur.csv`（模糊版）、`ablation.csv`、`metrics_blur.json`。

**实际意义**：这是方法文档 §14 要求的"最小可证伪版本"——在零下载成本下，用 GQA 场景图自建真假关系声明（280 条，140 真 / 140 假，117 张图，15 种关系），只回答一个问题：**在最难判断的场景里（模型最自信的声明），把关键部件遮掉之后，真话和幻觉能不能被分开？** 如果连这都分不开，方法就应该直接停下。

**结论**（blur 正式版）：

- **原始置信度基本是废的**。总 AUROC 只有 0.592，模型最自信的三分之一声明里掉到 0.537，跟抛硬币差不多。也就是说幻觉根本没法靠"模型听起来没底气"抓出来——自信幻觉是真的存在，而负控制差分正是该在这种地方出手。
- **核心假设在目标场景里成立**。高置信度档内，μ（标准化共同支持）把真假分开，AUROC 0.688（灰块 0.645），未标准化的"平均证据掉价"0.696。遮掉部件之后，真声明掉分明显多于假声明。
- **遮挡方式选型**：blur 最强（AUROC 0.752），mean 和 gray 相当（0.700 / 0.699）。blur 还修掉了一个灰块的符号异常（反演-subject 算子从 −0.016 翻正到 +0.078）——灰块把语义换成一块灰，可能被模型当成"另一个物体"重新解释，blur 保留了空间位置、只抹语义，更接近"去掉这个实体"的本意。
- **两个不能回避的硬伤**：
  - 非空间族（wearing / holding / riding / carrying / watching）在高置信度档只有 8 条声明，而且**全部为真**，根本算不出分离能力——而这恰恰是 COVER 要治理的主要家庭。这个缺口是 GQA 补不上的。
  - 最低置信度档的证据差分反向、无信息（μ 0.427–0.467）：对已经说 No 的假声明，遮了它更坚定 No。

**对 COVER**：**闸门第一步通过（成功）**——"负控制差分在原始置信度失效的自信区仍然可分"这个核心假设存活了，方法值得继续。但样本量太薄，最关键的非空间族没验证到（失败/未验证），必须换基准。

---

## 实验三：R-Bench 样本闸门（500 条）—— "换真基准补样本"

**路径**：`rbench_prepare.py` → `rbench_claims.csv`；`cover_rbench.py` → `scored_rbench_claims.csv`；关键教训是那一次逆关系修复重跑：`scored_rbench_invfix.csv`、`metrics_rbench_invfix.json`、`run_rbench_invfix.log`。数据在 `/root/autodl-tmp/data/R-bench/`。

**实际意义**：GQA 是自建标签，说服力有限。R-Bench 是公开基准，自带 s/o 方框和人工标注的真假，而且关系题大多属于非空间族——正好补 GQA 验证不了的缺口。先在 500 条样本上试水。

**结论**：

- **一个工程坑，但踩得很值**。第一版逆关系写成了"同动词交换论元"（比如"眼镜穿着女人"），导致高档 inversion 算子在数据里反着走。改成方法文档定义的真正逆关系（worn by / held by / sat on by 这类）之后才恢复。这个教训后来写进了文档口径。
- **方向复现**：非空间族高置信度档 μ 0.638 vs 原始置信度 0.482——跟 GQA 一个方向，还更强。但 bootstrap 区间仍跨过 0.5，样本量还不足以说"显著"。
- 空间族已经开始出现麻烦的苗头（总体 μ 0.588 vs a_orig 0.646，高档甚至反向 0.320）。

**对 COVER**：**方向性复现（成功）**，但样本量还是不够下最终结论（待定），顺手把 R-Bench 的清洗管道跑通了。

---

## 实验四：R-Bench 全量闸门（1335 条）—— "目标场景的基准级证据"

**路径**：`rbench_prepare.py --target 0` → `rbench_claims_full.csv`（1335 条，calib 520 / eval 815）；`cover_rbench.py --target 0` → `scored_rbench_full.csv`（完成打分 1331 条，eval 812 = 692 真 / 120 假）；分析产出 `metrics_rbench_full.json`、`eval_rbench_full.csv`、`run_rbench_full.log`。

**实际意义**：这是把 GQA 上"样本太少、非空间族验证不了"的口子正式补上的实验。1335 条干净声明里有 950 条非空间、385 条空间，覆盖 GQA 够不着的全部关系家族。它回答的是**方法的前提在真基准上到底成不成立**。

**结论**（关键数字）：

- **自信幻觉的签名在真基准上完整复现**。原始置信度随自信度单调退化：档内 a_orig 从 0.605 → 0.585 → 0.519。模型越自信的声明，越难靠原始置信度判断真假。
- **这是全项目第一个、也是唯一一个基准级正向证据**：非空间族高置信度档（raw 反转 0.466<0.5，模型最自信反而更可能是假），μ6 翻成 0.713，95% CI 下限 0.529 仍大于 0.5。**在 COVER 声称要治理的场景里，负控制差分把真假分开了，且强于原始置信度。**
- 算子信号全面变强：全 6 算子 g=1.6–2.2（GQA 只有 0.02–0.13），远超方法文档的 0.25 阈值。
- inversion 正控制有可测增量：非空间高档 μ3 0.627 → μ6 0.713，正控制不是摆设。
- **四个必须记录的问题**：
  - 空间族高档 μ 反转（0.400）——该族当前的控制/标准化口径不对，COVER 的声明必须限定非空间族，或为空间族单独设计。
  - J（过度识别冲突）的独立价值仍然没有证明：它的 AUROC（0.579）低于 μ；A=−μ+λJ 有尺度伪影（A 读数 0.312≈1−μ，因为 J 量级太小，λJ 项基本不参与）。
  - 高档假声明只有 23 条（非空间 11 / 空间 12），CI 偏宽——全档 μ 的区间 [0.489, 0.742] 含 0.5，**显著的是"非空间高档"这一个格子**。
  - **这是必要条件，不是充分条件**。AUROC 只证明"存在某个阈值能分开真假"，没有证明"这个阈值能找得到、找得到就能用"。

**对 COVER**：**必要条件确认（最大的成功）**——方法要成立的前提，在真实基准的目标场景里拿到了正向证据。但充分性完全没测，J 和空间族是两颗明雷。

---

## 实验五：决策层实验（split-conformal）—— "从能分得开，到能不能用"

**路径**：`score_inv_orig.py` → `a_inv_orig.csv`（对逆问题打分，用于测 REPAIR 输出翻转）；`decision_layer_rbench.py` → `decision_rbench.json`（Part A 验证轴 / Part B 双候选 REPAIR）；`decision_layer_stratified.py` → `decision_rbench_stratified.json`（Part C 按置信度分层）；`sensitivity_fullcalib_rbench.py` → `decision_rbench_fullcalib_sens.json`（敏感性）。跑时日志 `run_inv_orig.log`。

**实际意义**：前四个实验测的都是"排序力"——把真假声明排个序，看能不能分开。但方法最终要做的不是排序，而是**对每一条声明给出一个动作**：ACCEPT（信）、REPAIR（改成另一个关系）、ABSTAIN（拒绝回答）。决策层实验做的就是这最后一步：用 split-conformal 把得分校准成覆盖集（保证"真声明被接受"的概率 ≥ 1−δ），再套决策规则。**这一步过不去，前面的排序力就只是论文里的图，不是能用的方法。**

**结论**（三层数据全部一致，负结果）：

- **conformal 包装器是干净的（唯一的成功面）**。覆盖保证在族内全部成立（非空间 δ=0.05 实测 0.986 ≥ 0.95），说明标准化、标定、拆分的实现没有 bug。但这验证的是"任何得分套上这套流程都保覆盖"，不是 COVER 的分辨力。
- **拒绝力弱于原始置信度**。同样 δ 下，COVER 接受的假声明比例系统性更高（非空间 δ=0.05：COVER 0.970 vs raw 0.836；δ=0.10：0.821 vs 0.746）。要追平 raw，得把 δ 放宽到 0.20（意味着牺牲 20% 的真声明）。
- **REPAIR 机制失灵**。双候选集（{原关系, 逆关系}）决策下，REPAIR 有 87–91% 落在**真**声明上（非空间 δ=0.10：32 次 REPAIR 里 29 次是真声明），而且模型对修复后的逆问题几乎总是答 Yes（翻转率 ~100%）——等于在主动把真声明"纠正"成它们的逆。更要命的是 R-Bench 根本没有替代关系的真值（只有 6 条同 (s,o) 对的兄弟题），**连"REPAIR 到底修没修对"都无法判定**。
- **闸门最强的格子，在决策层反而全崩**。Part C 分层后，非空间高档（闸门里 μ=0.713、raw 反转的格子）COVER 风险 1.000——11 条假声明全部被接受，比 raw（0.818）差得多。**AUROC 分离没有转化为可工作的操作点**：conformal 保的是"真声明覆盖"，把阈值推到了假声明分布之上。
- **λ=1（把 J 加进来）在空间族直接爆炸**：q 飙到 7.5，风险 0.962，决策层等于"什么都信"。

**对 COVER**：**充分性检验没有通过（最大的失败）**。"能分辨真假"和"能据此做出正确决策"是两回事，决策层这一跳没跳过去。这同时反过来说明了一个更深的点：AUROC 衡量的"存在阈值"和 conformal 需要的"阈值可用"是两个问题，论文里不能把前者当后者。

---

## 总账：COVER 哪些成了，哪些没成

**成了的（必要条件，可以放心写进论文的）**：

1. **核心假设站住了**：负控制差分能把真假分开，而且恰好是在原始置信度失效的最自信区——GQA（μ 0.688）和 R-Bench 全量（非空间高档 0.713）两次复现，还用了公开基准的人工标注。
2. **目标场景拿到了基准级正向证据**：非空间族自信幻觉，raw 反转 0.466，μ6 翻到 0.713 [0.529, 0.876]。这是"COVER 在它声称要治理的地方有效"的第一个、也是唯一一个直接证据。
3. **工程与口径是干净的**：遮挡方式（blur）、逆关系口径（worn by 而非同动词交换）、split-conformal 实现（覆盖保证成立）都经得起检查。

**没成的（充分性缺口，必须解决或诚实披露的）**：

1. **决策层不会拒绝**：在保覆盖的操作点上，COVER 接受的假声明比 raw 更多；最有把握的高档反而全收（风险 1.000）。
2. **REPAIR 没有可用性**：盯着真声明改，翻转率 ~100%，而且 R-Bench 给不了真值测试台。
3. **空间族整套口径不对**：高档 μ 反转（0.400），λ=1 时 q 爆炸。
4. **J 冲突项至今没有证明独立价值**，λJ 还有尺度伪影。
5. **统计底气仍偏薄**：整个项目最关键的结论（非空间高档 0.713）建立在高档仅 23 条假声明之上，CI 不算宽也不窄，需要更多假声明。

**一句话总结**：COVER 的**眼力**（辨别真假的能力）得到了初步验证，但它的**手脚**（根据辨别结果去接受、去修复、去拒绝）还没证明能用。目前对"COVER 是否有效"的诚实回答是：**未确认有效**——前提在，机制不全。

---

## 下一步（按优先级，路径已就位）

1. **REPAIR 真值实验（已设计，未运行）**：GQA 场景图自带 `present_rels`（每个实体对真实存在的关系集合），用它当候选集，才能回答"REPAIR 修对了没有"——R-Bench 永远回答不了这个问题。需要新一轮服务器推理（对真实替代关系打分）。
2. **决策规则重设计**：把操作点从"保真声明覆盖"改成"保假声明风险"（risk-control / Neyman–Pearson 风格）——现在的 conformal 保的东西和我们真正要的东西是反的。
3. **J 的增量消融**：在 μ 相近的声明对上单独看 J 能不能分开真假。
4. **空间族控制重设计**：论文的 COVER 声明必须限定非空间族，或为空间族单独设计控制。

**复现提示**：本地 `/home/user/.clawsgo/projects/20862c13-0989-4239-93c6-07b489a7dfc8/tmp/cover_min_exp/` 有全部脚本与结果 JSON 的副本；工作区有各轮实验的正式报告与配图（`COVER_最小可证伪实验报告.md`、`COVER_Rbench全量_闸门报告.md`、`COVER_决策层实验报告.md` 及对应的 PNG）。
